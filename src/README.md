This directory is reserved for future non-header simplex implementation files.

The current extracted simplex project is header-only for the core solver, so the
active sources live in `include/simplex/` and `bindings/`.
